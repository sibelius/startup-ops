import { dark } from 'mdx-deck/themes';

export default {
  ...dark,
  colors: {
    ...dark.colors,
    background: '#03d69d',
  },
  font: "'Nunito', sans-serif",
  monospace: "'Cascadia Code', monospace",
};
